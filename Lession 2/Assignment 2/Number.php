<?php
// ใช้โครงสร้างการควบคุมการทำงานซ้ำ for เพื่อแสดงผลตัวเลขที่เพิ่มขึ้นทั้งแนวนอนและแนวตั้ง
for ($row = 1; $row <= 40; $row++) {
    for ($col = 1; $col <= $row; $col++) {
        echo $col;
    }
    echo "<br>";
}
?>